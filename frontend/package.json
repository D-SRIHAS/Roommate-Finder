{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.2.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.8.3",
    "css-select": "^5.1.0",
    "nth-check": "^2.1.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^6.3.0",
    "react-scripts": "^3.0.1",
    "resolve-url-loader": "^5.0.0",
    "socket.io-client": "^4.8.1",
    "svgo": "^3.3.2",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider npm run build:css && NODE_OPTIONS=--openssl-legacy-provider react-scripts start",
    "build": "NODE_OPTIONS=--openssl-legacy-provider npm run build:css && NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "build:css": "tailwindcss -i src/index.css -o src/tailwind.output.css",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "node-notifier": "^10.0.1",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "tough-cookie": "^5.1.2"
  }
}
